// import * as vscode from "vscode";
//
// console.log(`Loading: init-apc.js`);
//
// const config = vscode.workspace.getConfiguration();
// const output = vscode.window.createOutputChannel('init.ts')
//
// // TODO TODO Fix lost-focus bug when a terminal editor closes because the process exits (e.g. ^D in a shell)
// function f(editor) {
//   if (!editor) return;
//   console.log(`editor: ${editor}`);
//   // vscode.commands.executeCommand('workbench.action.focusActiveEditorGroup');
// }
// vscode.window.onDidChangeActiveTextEditor(f);
// vscode.window.onDidChangeActiveTerminal(f);
// vscode.window.onDidChangeActiveNotebookEditor(f);
//
// console.log(`Done loading: init-apc.js`);
